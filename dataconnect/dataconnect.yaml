specVersion: "v1"
serviceId: "furlog"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "furlog-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
